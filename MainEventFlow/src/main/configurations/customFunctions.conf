name = "customFunctions.conf"
type = "com.tibco.ep.streambase.configuration.sbengine"
version = "1.0.0"
configuration = {

	// An EventFlow custom function group is an associative array of custom function definitions, indexed by name.
	CustomFunctionGroup = {

		// An associative array of custom function definitions, indexed by EventFlow-visible function name. Function names
		// must conform to Java identifier semantics, but need not match the underlying Java method name. This key is
		// required.
		customFunctions = {

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			AddToFieldList = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ParseUtil"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "AddToFieldList"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			MergeTrigEvents = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.can.TrigData"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "MergeTrigEvents"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			GetChangedTrigEvents = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.can.TrigData"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetChangedTrigEvents"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			ContainSameTrigEvents = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.can.TrigData"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "ContainSameTrigEvents"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			GetEventFromTrigList = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.can.TrigData"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetEventFromTrigList"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			GetEventValueFromTrigList = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.can.TrigData"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetEventValueFromTrigList"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			AssignODTParams = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ParseUtil"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "AssignODTParams"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			AssignPolicyParams = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ParseUtil"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "AssignPolicyParams"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			UniqueDatas = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.UniqueDatas"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "aggregate"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			GetAWSUrl = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.adapters.AWSAdapter"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetAWSUrl"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			GetAuthorization = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.adapters.AWSAdapter"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetAuthorization"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			GetCurrUTCTimeString = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.TimeUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetCurrUTCTimeString"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			GetFilenameListInPath = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.FileUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetFilenameListInPath"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			GetDirnameListInPath = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.FileUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetDirnameListInPath"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			GetFilePathsInPath = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.FileUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetFilePathsInPath"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			GetDirPathsInPath = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.FileUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetDirPathsInPath"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			GetFilenameInPath = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.FileUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetFilenameInPath"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			GetKeyFlag = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ParseUtil"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetKeyFlag"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			GetLocalTimeString = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.TimeUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetLocalTimeString"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			GetTimestamp = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.TimeUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetTimestamp"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			GetMatchedSignal = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ParseUtil"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetMatchedSignal"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			UpdateVehicleSignalStatus = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.VehicleUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "UpdateVehicleSignalStatus"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			GetVehicleSignalStatus = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.VehicleUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetVehicleSignalStatus"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			GetTupleKeys = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ParseUtil"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetTupleKeys"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			CompareTupleData = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ParseUtil"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "CompareTupleData"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			GetODTParams = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ParseUtil"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetODTParams"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			GetPolicyParams = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ParseUtil"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetPolicyParams"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			GetTrigData = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ParseUtil"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetTrigData"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			GetValueInFieldList = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ParseUtil"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetValueInFieldList"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			InitODTRepository = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ParseUtil"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "InitODTRepository"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			InitPolicyRepository = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ParseUtil"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "InitPolicyRepository"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			convertHexToStr = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.StringUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "convertHexToStr"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			convertStrToHex = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.StringUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "convertStrToHex"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			convertStrToHexWithCharset = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.StringUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "convertStrToHexWithCharset"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			GetNotiMessage = {
				
				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.StringUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetNotiMessage"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getBigByteArrayFromInt = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.NumUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "getBigByteArrayFromInt"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getBigByteArrayFromLong = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.NumUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "getBigByteArrayFromLong"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getCCPCellDatas = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ccp.CCPCustomUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetCellTupleData"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getCCPCellDiff = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ccp.CCPCustomUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "getCellDiff"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getCCPDVol = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ccp.CCPCustomUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetDVol"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getCCPDeltaValue = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ccp.CCPCustomUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetDeltaValue"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getCCPMatchedTupleByInterval = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ccp.CCPCustomUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "getMatchedTupleByInterval"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getCCPMaxCDiffCells = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ccp.CCPCustomUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetMaxCDiffCells"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getCCPMaxCDiffVal = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ccp.CCPCustomUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetMaxCDiffVal"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getCCPMaxDVolCells = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ccp.CCPCustomUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetMaxDVolCells"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getCCPMaxDVolVal = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ccp.CCPCustomUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetMaxDVolVal"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getCCPMaxFieldValue = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ccp.CCPCustomUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetMaxValue"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getCCPMinFieldValue = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ccp.CCPCustomUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetMinValue"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getCCPMsrTBDatas = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ccp.CCPCustomUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "GetMsrTBTupleData"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getIntFromBig = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.NumUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "getIntFromBig"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getIntFromLittle = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.NumUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "getIntFromLittleWithSigned"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getLittleByteArrayFromInt = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.NumUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "getLittleByteArrayFromInt"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getLittleByteArrayFromLong = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.NumUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "getLittleByteArrayFromLong"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getLongFromBig = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.NumUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "getLongFromBig"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getLongFromLittle = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.NumUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "getLongFromLittleWithSigned"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			getDoubleFromStr = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.NumUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "getDoubleFromStr"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			getLongFromStr= {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.NumUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "getLongFromStr"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			getThreadId = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.common.ThreadUtil"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "getThreadId"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			joinCCPCellTuples = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ccp.CCPCustomUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "JoinFromCellTuples"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			joinCCPMsrTBTuples = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ccp.CCPCustomUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "JoinFromMsrTBTuples"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}

			// Configuration for a single custom function. Function names must conform to Java identifier semantics.
			joinCCPNumberList = {

				// Set this key to true to have the EventFlow engine autodetect the arguments and return type of the current
				// function. If you specify arguments and/or a return type, then this key is ignored. This key is optional and its
				// default value is false.
				autoArguments = true

				// The fully qualified class name where the named public static function is found. This key is required.
				className = "com.autoever.poc.parser.ccp.CCPCustomUtils"

				// Function method name. This key is required if type is 'simple', optional with required value 'accumulate' if type
				// is 'aggregate'
				methodName = "JoinFromNumberList"

				// Function type, one of 'simple' or 'aggregate'. This key is optional and its default value is 'simple'.
				type = "simple"
			}
		}
	}
}
